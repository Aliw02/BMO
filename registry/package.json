{
  "name": "bfp-registry",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "vitest": "^3.1.1",
    "wrangler": "^4.0.0"
  }
}
